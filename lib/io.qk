usize extern fwrite<T>(T* ptr, usize size, usize nitems, File* stream);

import lib::string;

void print(str message) {
	fwrite(message.slice.data, message.slice.size, sizeof(char), extern<File*> "stdout");
	char* newline = "\n";
	fwrite(newline, 1, sizeof(char), extern<File*> "stdout");
}
